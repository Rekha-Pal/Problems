#include <stdio.h>
int main()
{
    long long int n;
    scanf("%lld",&n);
    while(n>1)
        if(n%2==0)
            n/=2;
        else
            break;
    printf(n==1 ? "TAK" : "NIE");
    return 0;
}
