#include <stdio.h>
int main()
{
    int n,sq;
    scanf("%d",&n);
    while(n)
    {
        sq=(n*(n+1)*(2*n+1)/6);
        printf("%d\n",sq);
        sq=0;
        scanf("%d",&n);
    }
    return 0;
}
