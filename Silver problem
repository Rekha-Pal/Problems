#include <stdio.h>
int main()
{
    int n,count=0;
    scanf("%d",&n);
    while(n)
    {
        count=0;
        while(n>1)
        {
            n= n%2 ? n-(n+1)/2 : n/2;
            count++;
        }
        printf("%d\n",count);
        scanf("%d",&n);

    }
    return 0;
}
